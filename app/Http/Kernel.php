<?php

namespace App\Http;

use Illuminate\Foundation\Http\Kernel as HttpKernel;

class Kernel extends HttpKernel
{
    /**
     * The application's global HTTP middleware stack.
     *
     * These middleware are run during every request to your application.
     * Middleware configuration has been moved to bootstrap/app.php in Laravel 12.
     *
     * @var array<int, class-string|string>
     */
    protected $middleware = [
        // Middleware is now configured in bootstrap/app.php
    ];

    /**
     * The application's route middleware groups.
     *
     * Middleware groups are now configured in bootstrap/app.php in Laravel 12.
     *
     * @var array<string, array<int, class-string|string>>
     */
    protected $middlewareGroups = [
        // Middleware groups are now configured in bootstrap/app.php
    ];

    /**
     * The application's middleware aliases.
     *
     * Middleware aliases are now configured in bootstrap/app.php in Laravel 12.
     *
     * @var array<string, class-string|string>
     */
    protected $middlewareAliases = [
        // Middleware aliases are now configured in bootstrap/app.php
    ];
}
